/**
 * @description ui参数配置文件
 */

export let canvas;
export let ctx;

export let canvasConf = {
    width: 100,
    height: 40,
    padding: 10
};

export let scrollConf = {
    width: 5,
    height: 0,
    radius: 2,
    color: '#D3D3D3'
};

export let cursorConf = {
    cursorTime: 500
};
