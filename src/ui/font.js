/**
 * @description font对象
 */

export default class Font {

    heightCount : number;
    fontArray: array;

    $computed: object; // 计算属性

    constructor (obj, ui) {
        this.heightCount = 0;
    }

    pushFont (font) {
    }
};
